apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "counterapp.fullname" . }}-test-connection"
  labels:
    {{- include "counterapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "counterapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
